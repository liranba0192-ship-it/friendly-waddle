# הרחבת ידע בוקר — תוספים, אימונים ותזונה 🌅📚

> זהו ה-prompt שטריגר מתוזמן (Scheduled Trigger) של Claude Code מריץ כל בוקר.
> המטרה: **תדריך קצר, מעשי ומעניין** — לא מאמר אקדמי. כ-3-4 דקות קריאה.
> אין צורך לערוך אותו ידנית — פשוט חבר אליו את הטריגר (ראה `SETUP.md`).

## המשימה

הפק **תדריך בוקר קצר בעברית** — 3 נושאים (תוסף/אימון/תזונה), כל אחד תמציתי ומעשי.
היקף היעד: קריאה של **כ-3-4 דקות** (בערך 450–650 מילים בסה"כ, לא לכל נושא). ישיר, ענייני, בלי מילוי.

## 🎯 רלוונטיות לפני הכל (חשוב יותר מחידוש!)

**הבעיה שתיקנו**: בעבר הרוטינה בחרה נושאים נישתיים ומשעממים רק כדי "לא לחזור על עצמה" (למשל תוספים אזוטריים שאף אחד לא לוקח). זה נגמר.

- בחר נושאים ש**אדם רגיל שמתאמן ואוכל בריא באמת ירצה לדעת** — לא נושאים נדחקים לפינה רק כי הם "עוד לא בפנקס".
- עדיף לחזור על **זווית חדשה** לנושא מוכר ופופולרי (למשל "קריאטין — מה חדש ב-2026") מאשר לקפוץ לתוסף אזוטרי שאין לו שימוש מעשי.
- אם אין באמת נושא מעניין וחדש בתחום מסוים היום — **מותר לדלג על התחום הזה היום** ולהביא רק 2 נושאים במקום 3, ולא "למלא" עם משהו לא רלוונטי.
- בדוק את `briefings/covered-topics.json` כדי לא לחזור מילה במילה על מה שכבר נכתב (אותו תוסף עם אותה זווית בדיוק) — אבל זה שיקול משני, לא הקריטריון המוביל.

## 🗣️ סגנון — ישיר ומעשי, לא הרצאה

1. תכתוב כמו שאתה מסביר לחבר, לא כמו מאמר מדעי. משפטים קצרים. בלי "פירוק כל מונח מאפס" אם המונח פשוט.
2. **התחל כל נושא ישר עם הפואנטה** — מה לעשות / מה המסקנה. אחר כך (בקצרה) הרקע אם צריך.
3. ראיות כן חשובות — אבל בלי ציטוט מוגזם. משפט אחד על רמת הביטחון מספיק ("מבוסס היטב" / "עדות ראשונית"), לא פסקה שלמה של דירוג ראיות לכל סעיף.
4. בלי פדינג. אם אין יותר מה להגיד על נושא — תעצור.

## ⛔ כללי-ברזל נגד מידע לא מבוסס (נשאר בתוקף מלא — הקיצור לא על חשבון הדיוק)
1. **אסור להמציא** מחקרים, שמות חוקרים, מספרים, מינונים או ציטוטים. אם לא מצאת מקור אמין — אל תכתוב את הטענה.
2. **כל מספר/מינון חייב לבוא ממקור שמצאת בפועל ב-WebSearch**, לא מהזיכרון בלבד.
3. **אסור להמציא קישורים.** צרף רק כתובות URL שראית בתוצאות החיפוש בפועל.
4. כשהראיות חלשות או סותרות — אמור זאת במשפט קצר ("הראיות עדיין מוגבלות") במקום להשלים פערים.
5. השתמש ב-WebSearch עם התאריך הנוכחי כדי לוודא עדכניות.

## 💾 שמירה לאפליקציה (חובה — בסוף כל הרצה)
לאחר שהפקת את התדריך, שמור אותו כך שאפליקציית ה-PWA (בתיקייה `app/`) תציג אותו:

1. **שמור את התדריך** כקובץ Markdown בנתיב `briefings/<YYYY-MM-DD>.md` (לפי **התאריך של היום**).
   התוכן = התדריך המלא בפורמט שלמטה.
2. **עדכן את `briefings/index.json`**: הוסף בראש מערך `briefings` רשומה חדשה
   `{ "date": "<YYYY-MM-DD>", "day": "<אות היום>", "title": "<כותרת קצרה של היום>", "file": "<YYYY-MM-DD>.md" }`.
   - שמור את כל הרשומות הקיימות. אם כבר קיימת רשומה לאותו תאריך — החלף אותה (אל תכפיל).
   - ודא שה-JSON נשאר תקין.
3. **עדכן את הפנקס `briefings/covered-topics.json`**:
   הוסף את הנושאים של היום, כל אחד לרשימה המתאימה (`supplements` / `training` / `nutrition`) — רק כדי למנוע חזרה מילולית, לא כדי לכפות חידוש מלאכותי.
   - אל תמחק נושאים קיימים — רק מוסיפים בסוף.
   - ודא שה-JSON נשאר תקין.
4. **בצע commit ו-push** של הקבצים ששונו (`<YYYY-MM-DD>.md`, `index.json`, `covered-topics.json`).
   הודעת commit לדוגמה: `Add daily briefing <YYYY-MM-DD>`.
5. ⚠️ **חובה מוחלטת — אל תסתפק ב-push!** הסביבה שלך עובדת על branch נפרד משלה, **לא על `main`**.
   אם תעצור אחרי ה-push, התדריך יישאר תקוע על branch יתום ולעולם לא יגיע לאפליקציה.
   לכן **מיד אחרי ה-push**, בצע את שני הצעדים הבאים באמצעות כלי ה-GitHub MCP (`mcp__github__create_pull_request` ואז `mcp__github__merge_pull_request`), **בתוך אותה הרצה**:
   - צור Pull Request מה-branch שלך אל `main`.
   - מזג אותו מיד (`merge_method: "squash"`).
   - אם המיזוג נכשל עם "merge conflicts" — הרץ `git fetch origin main && git merge -X ours origin/main --no-edit && git push`, ואז נסה למזג את ה-PR שוב.
   - **ודא בסוף שהמיזוג הצליח** (התגובה מכילה `"merged": true`). אם לא — נסה שוב עד שמצליח. אל תסיים את ההרצה בלי מיזוג מאושר.

> כך כל בוקר נוסף תדריך חדש לארכיון של האפליקציה, והתדריך של היום מקבל תג "היום". בלי השלב הזה שום דבר לא באמת מתעדכן אצל המשתמש.

## פורמט מומלץ
```
# 📚 הרחבת ידע בוקר — <תאריך> (יום <אות>)
*זמן קריאה משוער: ~3-4 דקות*

## 💊 תוסף היום: <שם>
<ישר לעניין: מה זה עושה, מינון, האם שווה לקחת. 3-5 משפטים.>

## 💪 עקרון אימון: <נושא>
<ישר לעניין: מה המסקנה המעשית. 3-5 משפטים.>

## 🥗 תזונה: <נושא>
<ישר לעניין: מה לעשות בפועל. 3-5 משפטים.>

## 🔑 שורה תחתונה
3 takeaways קצרים, משפט אחד כל אחד.

**מקורות:** [שם](url) · [שם](url) · ...
```
